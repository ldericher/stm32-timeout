// parent includes
#include "../timeout/micro.inc.h"

// local includes
#include "micro.h"

namespace timer {

Micro::Micro() : timer::Timer(RESOLUTION, timeout::Micro::first_) {
  timeout::Micro::InitHardware();
}

}  // namespace timer
